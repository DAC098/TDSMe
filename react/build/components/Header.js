var React = require('react');

const Header = React.createClass({

    render: function() {

        return (
            <header>
                <section>

                </section>
                <section>

                </section>
                <section>

                </section>
            </header>
        )
    }
});

module.exports = Header;
